global:
  message: welcome to customer prod environment!!
server:
  port: 6001
message: hello from prod
spring:
   datasource:
    url: jdbc:mysql://localhost:3306/cust_prod?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
 
